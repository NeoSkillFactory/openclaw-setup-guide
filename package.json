{
  "name": "openclaw-setup-guide",
  "version": "1.0.0",
  "description": "Interactive step-by-step setup assistant for OpenClaw with automated configuration and error handling.",
  "main": "scripts/cli-interface.js",
  "scripts": {
    "start": "node scripts/cli-interface.js",
    "detect": "node scripts/cli-interface.js --detect-only",
    "validate": "node scripts/cli-interface.js --validate-only",
    "test": "node --test scripts/__tests__/*.test.js"
  },
  "keywords": [
    "openclaw",
    "setup",
    "wizard",
    "configuration",
    "onboarding"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  }
}
